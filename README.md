Works with wired & wireless connection for DS4, you need at least Python 2.5.6 to run.

The program works in the background, you can begin/stop recording by pressing F1 key, pressing ESC key exits the program.

Parsed input will be outputted as .txt file in the location where you started the program with the following name convention:

ds4_log_(time stamp).txt
